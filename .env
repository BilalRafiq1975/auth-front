# API Base URL (defaults to production, can be overridden in local dev)
#VITE_API_URL=http://localhost:4000

# App Configuration
VITE_APP_NAME=Auth Portal
VITE_APP_ENV=development

# For production override the VITE_API_URL manually or via deployment config
# Uncomment the following for production:
VITE_API_URL=https://auth-back-production.up.railway.app
